```json
{
  "verdict": "APPROVED",
  "narrative": "## Scope reviewed\nOnly `highper-gateway/src/discovery/registry.rs` (uncommitted working-tree diff), against T-20260911-uc12-a-discovery-registry-respects-refre.md and the round-1 MAJOR finding (`tokio::time::interval` panics on `refresh_interval=0` in `watch_service`). Read `.claude/project-profile.md`, the task spec, the full file, `discovery/mod.rs` (DiscoveryConfig, trait), and the three `watch_service` trait implementations (consul.rs:208, etcd.rs:247, static.rs:276) to confirm none of them block, since `registry.rs:148` now runs before the early-return the revision adds.\n\n## Pass 1 - Correctness\nThe fix is sound. `backup_refresh_period` (registry.rs:137-142) returns `None` iff `config.refresh_interval == 0`, matching the same zero-check already used by `needs_refresh` (registry.rs:69-71), so the two decision points stay consistent. `watch_service` (registry.rs:145-172) now destructures with `let Some(period) = ... else { ...; return Ok(()); }` (registry.rs:151-157) BEFORE `tokio::time::interval(period)` is ever constructed (registry.rs:162), so the panicking input can no longer reach the ticker constructor. I confirmed `self.discovery.watch_service(service_name)` (registry.rs:148, called before the early return) is non-blocking in every implementation (consul.rs:208-212 just logs and returns; etcd.rs:247+ spawns its own task and returns; static.rs:276 - checked, trivial) so calling it ahead of the guard introduces no hang or ordering hazard. AC #2 (\"refresh_interval == 0 keeps the old behaviour\") is preserved: `needs_refresh` still forces a refresh on every `get_upstreams` call when the TTL is zero, so the absent backup ticker adds nothing, matching the new doc comment at registry.rs:135-136, which I verified is accurate against the code it describes.\n\n## Pass 2 - Language & project safety\nNo new unwraps, no new lock acquisitions, no new `unsafe`, no swallowed errors. The `let-else` is idiomatic and the `?` on `self.discovery.watch_service(...)` still propagates with the caller's service-name context via the enclosing `info!` logs. No secrets or sensitive data in the new log lines (service name only).\n\n## Pass 3 - Operational / test-coverage gap (new finding)\nThe new test `test_zero_refresh_interval_has_no_backup_ticker` (registry.rs:328-339) only exercises the extracted pure function `backup_refresh_period`; no test in the file calls `registry.watch_service(...)` with `refresh_interval=0` to prove the actual panic SITE is safe. That is defensible - the `tokio::time::interval` call sits inside a detached `tokio::spawn` whose `JoinHandle` is discarded (registry.rs:161-169), so a panic there would surface asynchronously in a task nobody awaits, not as a synchronous test failure; a true end-to-end regression test would need the spawn's handle kept and joined, which the revision doesn't do (pre-existing shape, not changed here). Net effect: a future edit that inlines `tokio::time::interval(Duration::from_secs(self.config.refresh_interval))` directly into `watch_service`, bypassing `backup_refresh_period` entirely, would reopen the exact MAJOR finding from round 1 and no test in this file would catch it - a mutation harness would not catch this either, since the gap is a missing scenario (call `watch_service` itself), not a mutated branch inside code any test reaches.\n\n## Judgment on the three deferred minors\nNone of them should block this revision:\n1. No exact `elapsed() == ttl` boundary test (needs_refresh, registry.rs:78) - the `>=` comparison is correct by inspection; this is a coverage gap, not a behavioural defect, and orthogonal to the panic fix.\n2. No single-flight at TTL expiry (get_upstreams, registry.rs:50-53 / refresh_service, registry.rs:84-119) - a real thundering-herd risk under concurrent callers at exactly-expired TTL, but it predates this diff and the task's scope was narrowly the panic; deferring is reasonable for a T2 file with no external callers today (per stated fact: ServiceRegistry has no caller outside its own file).\n3. needs_refresh's decision and get_upstreams's cache read are separate lock acquisitions (registry.rs:51 vs 56) - a genuine TOCTOU window, also pre-existing and unrelated to the zero-interval fix.\nI checked whether the backup-ticker fix interacts with #2 to make it worse (e.g. the ticker and an on-demand refresh racing every period) and concluded it doesn't materially change the pre-existing risk profile: the ticker already called `refresh_service` unconditionally before this revision; only the period's derivation moved into `backup_refresh_period`.\n\n## Required changes before testing\nNone blocking. Recommend (non-blocking) that a follow-up task add either (a) an integration test that spawns `watch_service` with `refresh_interval=0`, keeps the `JoinHandle`, and asserts it doesn't abort, or (b) a code comment on `backup_refresh_period` cross-referencing `watch_service` so a future refactor doesn't re-inline the ticker construction.\n\n## Questions for the coder\n- Is there an intended shutdown/cancellation path for the `tokio::spawn` loop in `watch_service` (registry.rs:161-169)? It runs forever with a discarded `JoinHandle`; out of scope for this diff but worth a tracked follow-up given `ServiceRegistry::clone()` keeps the `Arc`s (and thus the discovery backend) alive for as long as the task runs.\n\n## What I did not check\nDid not and could not compile or run the test suite - confirmed pre-existing per the task brief that the `--lib` target fails at `runtime/signals.rs:238`, unrelated to this file. Did not review `consul.rs`, `etcd.rs`, `static.rs` beyond their `watch_service` bodies (needed only to confirm non-blocking behaviour). Did not re-review the original design/decision record - out of scope per instructions.",
  "findings": [
    {"class": "unclassified", "severity": "minor", "lang": "rust", "pattern": "", "domain": "", "file": "highper-gateway/src/discovery/registry.rs", "line": 328, "summary": "New test only covers backup_refresh_period in isolation; no test calls watch_service(0) to guard the actual panic site"},
    {"class": "race", "severity": "minor", "lang": "rust", "pattern": "single-flight", "domain": "", "file": "highper-gateway/src/discovery/registry.rs", "line": 51, "summary": "Carried over from round 1: no single-flight guard, concurrent get_upstreams calls at TTL expiry can double-refresh (not fixed by this revision, correctly deferred)"},
    {"class": "race", "severity": "minor", "lang": "rust", "pattern": "check-then-act", "domain": "", "file": "highper-gateway/src/discovery/registry.rs", "line": 56, "summary": "Carried over from round 1: needs_refresh decision and the cache read are separate lock acquisitions, a TOCTOU window (not fixed by this revision, correctly deferred)"},
    {"class": "unclassified", "severity": "minor", "lang": "rust", "pattern": "", "domain": "", "file": "highper-gateway/src/discovery/registry.rs", "line": 78, "summary": "Carried over from round 1: no test at the exact elapsed()==ttl boundary for needs_refresh (not fixed by this revision, correctly deferred)"}
  ]
}
```