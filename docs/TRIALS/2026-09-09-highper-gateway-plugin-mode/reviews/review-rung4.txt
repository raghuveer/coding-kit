```json
{
  "verdict": "REVISE",
  "narrative": "## Scope reviewed\nOnly changed file per task: highper-gateway/src/discovery/registry.rs (needs_refresh, get_upstreams's refactor to call it, the #[cfg(test)] with_discovery constructor, and three new tests). Cross-checked highper-gateway/src/discovery/mod.rs (DiscoveryConfig, ServiceDiscovery trait, create_discovery) and grepped the whole highper-gateway/src tree for callers of ServiceRegistry / get_upstreams / refresh_service / clear_cache / watch_service. Read .project/tasks/T-20260911-uc12-a-discovery-registry-respects-refre.md and docs/planning/ROADMAP.md:445-458.\n\n## Pass 1 -- Correctness\nThe core TTL logic in needs_refresh (registry.rs:68-81) is correct against the spec: refresh_interval==0 always refreshes (line 70-72, satisfies AC2), an uncached service always refreshes (73-75), a cached service refreshes only once `elapsed() >= ttl` (76-80, satisfies AC1 with the correct inequality direction for 'at least N seconds old'). get_upstreams (50-61) wires this in correctly and error/empty-instance propagation through refresh_service (84-119, unchanged) is untouched, satisfying AC3. Traced both guard directions (empty cache, missing last_update entry, stale entry, fresh entry) and all resolve to the safe/refresh side, not a fail-open. Traced the two-map (upstreams / last_update) interleavings against clear_cache and refresh_service; every interleaving I found resolves to 'needs_refresh=true' (extra refresh), never to serving from a half-written state as fresh -- no fail-open found here.\n\nOne real defect: registry.rs:66-67's new doc comment and the new test at 301-309 (test_zero_refresh_interval_refreshes_every_call) establish `refresh_interval == 0` as a legitimate, explicitly tested configuration value for a ServiceRegistry. But watch_service (134-155), unchanged and in the same file, builds its backup ticker with `tokio::time::interval(Duration::from_secs(registry.config.refresh_interval))` at line 145 -- `tokio::time::interval` panics if the period is zero. A ServiceRegistry configured with refresh_interval=0 (now blessed by this diff's own comment and test as correct) will panic the task that calls watch_service on it. This is exactly the pattern of fixing/legitimizing a value at one site without checking a sibling consumer of the same field in the same file -- the diff's own grep-scope (per task instructions) would have surfaced it. Blast radius today is low only because ServiceRegistry has no live caller anywhere in highper-gateway/src outside its own test module (confirmed by grep) and the file header (registry.rs:6) says the module is 'temporarily not actively used' -- but watch_service is `pub`, part of the crate's public surface, and the new tests actively encourage treating 0 as safe.\n\n## Pass 2 -- Language & project safety\nNo unjustified type escapes, no bare catch-and-swallow (refresh_service's `?` propagates; the empty-instance branch was pre-existing and unchanged, matching AC3 verbatim). Locking pattern (`RwLock<DashMap<_>>.read().await` used even for DashMap inserts/removes) is pre-existing repo convention, not introduced by this diff, and is internally consistent with itself. `with_discovery` is correctly gated `#[cfg(test)]` and private, so it cannot leak into the production constructor surface.\n\n## Pass 3 -- Operational\nnew needs_refresh + get_upstreams path has no single-flight/coalescing: concurrent callers who all observe a just-expired TTL will each independently call refresh_service, i.e. simultaneous backend calls for the same service (registry.rs:51-53 / 68-81). This is a much smaller window than the pre-fix always-refresh behaviour it replaces, and the roadmap scoped this as a ~half-day quick fix, so I am not blocking on it, but it is worth naming since discovery backends (Consul/etcd) are exactly where an unbounded thundering herd at TTL expiry under load would first be felt, and the diff adds no metric/log around refresh frequency to make that observable. No secrets in the new log lines (service names only, unchanged log statements). No new resource-cleanup-on-throw concerns.\n\n## Required changes before testing\n1. Fix or explicitly guard the refresh_interval==0 case in watch_service (registry.rs:143-145), e.g. skip the periodic-ticker spawn (or pick a sane floor) when config.refresh_interval==0, since needs_refresh and the new test now declare 0 to be valid input for the same struct.\n2. Add (or explicitly note as a deliberate gap) a test at the exact TTL boundary (`elapsed() == ttl`) -- see mutation-coverage note below.\n\n## Questions for the coder\n- Was watch_service considered when deciding to explicitly support and test refresh_interval==0, or was 0 only ever exercised through get_upstreams?\n- Is ServiceRegistry actually planned to be wired into gateway startup for GA (roadmap UC12 is 'remaining for v1.0 GA'), or does it stay dead code -- this changes how urgently finding #1 needs to be closed.\n\n## What I did not check\n- Did not check docs/adr or docs/design-input (task cites only the roadmap line and the task file itself; no ADR reference given).\n- Did not inspect consul.rs / etcd.rs / static.rs implementations of ServiceDiscovery beyond confirming the trait signatures the mock in the diff must satisfy.\n- Could not compile or run the new tests (per the task's stated facts: --lib target does not build at this commit due to an unrelated pre-existing error in runtime/signals.rs); all conclusions above about the three new tests are from static reading, not execution.\n- Did not evaluate whether ServiceRegistry being unwired is itself acceptable for this task's scope -- that is a design-level fact for approach-reviewer, not something to fix here.\n\n## What a mutation harness could not have caught\n- A mutant flipping `>=` to `>` at registry.rs:78 survives all three new tests, because the only 'after TTL' test backdates the clock by 31s against a 30s TTL (31s slack either way) -- there is no test at exactly `elapsed() == ttl`. This is a coverage gap mutation testing would normally expose if a boundary test existed; here it is invisible because the scenario itself was never written.\n- watch_service (registry.rs:134-155) has zero test coverage of any kind in this diff -- no mutation of that method's branches is exercised by any test, so the refresh_interval==0 panic is invisible to any mutation harness regardless of how thorough it is; only reading the method against the new comment's claim surfaces it.\n- No test exercises two concurrent get_upstreams calls racing across a TTL expiry boundary; this is a scenario gap, not a branch a mutation tool would ever generate on its own."
,
  "findings": [
    {"class": "correctness", "severity": "major", "lang": "rust",
     "pattern": "shared-config-divergent-consumer",
     "domain": "",
     "file": "highper-gateway/src/discovery/registry.rs", "line": 145,
     "summary": "watch_service's tokio::time::interval panics on refresh_interval=0, a value the diff's own comment/test now declare valid"},
    {"class": "correctness", "severity": "minor", "lang": "rust",
     "pattern": "cache-ttl-boundary",
     "domain": "",
     "file": "highper-gateway/src/discovery/registry.rs", "line": 78,
     "summary": "No test at elapsed()==ttl boundary; a >= to > mutation on this line survives all three new tests"},
    {"class": "race", "severity": "minor", "lang": "rust",
     "pattern": "cache-thundering-herd",
     "domain": "",
     "file": "highper-gateway/src/discovery/registry.rs", "line": 51,
     "summary": "No single-flight around refresh: concurrent callers at TTL expiry each independently call the discovery backend"}
  ]
}
```