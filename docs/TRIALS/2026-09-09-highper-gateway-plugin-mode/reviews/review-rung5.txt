```json
{
  "verdict": "REVISE",
  "narrative": "## Scope reviewed\nOnly changed file: highper-gateway/src/discovery/registry.rs (per task: get_upstreams, new needs_refresh, #[cfg(test)] with_discovery, three new tests). Cross-checked highper-gateway/src/discovery/mod.rs (ServiceDiscovery trait, DiscoveryConfig, default_refresh_interval) and grepped the whole highper-gateway/src tree for every caller of ServiceRegistry/get_upstreams/refresh_service/clear_cache/watch_service. Confirmed: ServiceRegistry is not wired into any live caller anywhere in the crate today (mod.rs:23 re-exports it, nothing else imports it) — this matches the file's own header comment at registry.rs:5-6 ('temporarily not actively used'). Also read .claude/project-profile.md for tier rules (discovery/** has no explicit tier.rule, falls to tier.default: T1, but the task self-declares T2, which is a floor override, consistent with 'blast radius unknown' framing) and docs/planning/ROADMAP.md:453-458 (UC12.A) for the source requirement.\n\n## Pass 1 -- Correctness\nnew `needs_refresh` (registry.rs:68-81) is logically sound against the acceptance criteria: ttl==0 short-circuits to always-refresh (:70-72, matches AC2), absence in the `upstreams` cache forces refresh (:73-75), and `elapsed() >= ttl` correctly implements 'at least refresh_interval seconds old' (:78). `get_upstreams` (:50-61) still propagates refresh errors via `?` and still ends in 'Service not found' on an empty instance list, matching AC3 -- I traced the empty-list early return in `refresh_service` (:93-96) and confirmed neither `upstreams` nor `last_update` get written on that path, so behaviour there is unchanged pre/post fix.\n\nThe defect is a consumer gap, not a defect in the changed lines themselves (failure mode (i) from the brief: fix at the SITE, consumer left unchanged). `needs_refresh` (:70-72) now makes `refresh_interval == 0` an explicit, tested, sanctioned value for `ServiceRegistry` -- test_zero_refresh_interval_refreshes_every_call (:301-309) locks this in. But `watch_service` (:134-155), unchanged by this diff, reads the exact same `self.config.refresh_interval` field to build a ticker: `tokio::time::interval(Duration::from_secs(registry.config.refresh_interval))` at :145. `tokio::time::interval` panics if the period is zero. Before this diff, `refresh_interval` had no effect on `get_upstreams` at all (always refreshed), so a caller who configured `refresh_interval: 0` for 'always fresh' semantics and then called `watch_service` would already hit this panic -- but nothing in the codebase legitimised 0 as a real configuration value for this struct. This diff's own tests now assert 0 is correct, supported behaviour for `ServiceRegistry`, which reads as 'refresh_interval=0 is safe here' to the next engineer, while the sibling method on the same struct, reading the same field, will crash the moment it's exercised with that value. No test in this diff calls `watch_service` at all, so this consumer path is untouched and unobserved.\n\n## Pass 2 -- Language & project safety\nNo unsafe type escapes. `with_discovery` (:39-47) is correctly gated `#[cfg(test)]`. Async paths are fully awaited; no bare catches (this is Rust, no swallowed Result -- `?` used correctly throughout). Lock usage (`RwLock<DashMap<...>>`, `.read().await` used even for DashMap inserts/removes) is an existing pattern reused consistently, not something introduced here.\n\n## Pass 3 -- Operational\nTwo related gaps, both pre-existing shape but newly relevant given the TTL feature:\n1. Check-then-act race: `needs_refresh` (:51) and the subsequent cache read (:56-60) are two separate lock acquisitions; a concurrent `clear_cache` between them can turn a 'should be cached' read into a spurious 'Service not found', or a concurrent refresh can duplicate backend calls. Same shape existed before (old code always refreshed then read separately), so not newly introduced, but worth naming since the new TTL logic is exactly the kind of code that invites assuming atomicity it doesn't have.\n2. No stampede protection: when the TTL boundary is crossed under concurrent load, every in-flight caller for the same service_name independently observes needs_refresh()==true and independently calls the discovery backend (:52). This is a real regression risk if/when this module is wired into the hot path, since the whole point of adding a TTL cache is to reduce backend load, but a burst of concurrent requests right at expiry defeats that.\n\nNeither of these blocks the specific acceptance criteria in the task file, but they are real latent risks in code the module's own header says is 'temporarily not actively used' -- i.e., due to be wired in later, at which point these become live.\n\n## Required changes before testing\n1. (Major) Fix or guard `watch_service`'s ticker construction (registry.rs:145) for `refresh_interval == 0` before or alongside sanctioning that value in `needs_refresh` -- e.g. treat 0 as 'no periodic backup refresh' (skip the ticker) rather than passing it straight to `tokio::time::interval`. Add a test that exercises `watch_service` with `refresh_interval: 0` so this consumer is checked the same way `get_upstreams` now is.\n2. (Minor, non-blocking) Consider adding a boundary test at `elapsed() == ttl` exactly (current test backdates by ttl+1s, leaving the `>=` vs `>` boundary unexercised by any test -- see 'what a mutation harness could not have caught' below).\n\n## Questions for the coder\n- Was `watch_service` considered when deciding to make `refresh_interval == 0` a supported value, or was that consequence of AC2 not traced through the rest of the struct?\n- Is there a plan (tracked elsewhere) for singleflight/coalescing on the refresh path before this registry is wired into a live caller, or is that accepted as future work given current dead-code status?\n\n## What a mutation harness could not have caught\nA scenario gap, not a branch gap: no test ever calls `watch_service`, so a mutation harness scoring coverage of the changed lines would never touch the panic path in :145 -- the harness only mutates branches the existing tests reach, and this consumer is reached by zero tests, old or new. Also: the TTL comparison test only exercises `elapsed() (31s) >= ttl (30s)` with a 1s margin, so a mutation from `>=` to `>` at line 78 would survive undetected; only the exact-equality boundary would catch it.\n\n## What I did not check\n- Did not check whether `refresh_interval == 0` is even a value operators are expected to configure in practice (no config schema/docs for DiscoveryConfig defaults reviewed beyond mod.rs:82-127).\n- Did not verify tokio's exact panic message/behaviour for `interval(Duration::ZERO)` against the vendored tokio version in this workspace's Cargo.lock -- I could not run code per task constraints; this is stated as recollection of documented tokio behaviour, not a verified run.\n- Did not review consul.rs/etcd.rs/static.rs internals beyond confirming they are not touched by this diff and are not additional callers of the changed methods.\n- Did not assess whether the CLAUDE.md citation/refusal policy in this repo root applies to this review task; it reads as authored for a document-QA workflow, not code review, and I deferred to my own reviewer contract instead of trying to force that policy's mechanics (quotation marks, refusal tokens) into a structured findings review.",
  "findings": [
    {"class": "correctness", "severity": "major", "lang": "rust",
     "pattern": "", "domain": "",
     "file": "highper-gateway/src/discovery/registry.rs", "line": 145,
     "summary": "needs_refresh now sanctions refresh_interval=0 (tested), but watch_service's tokio::time::interval on the same field panics on zero"},
    {"class": "race", "severity": "minor", "lang": "rust",
     "pattern": "", "domain": "",
     "file": "highper-gateway/src/discovery/registry.rs", "line": 51,
     "summary": "needs_refresh check and the later cache read/insert in get_upstreams are separate lock acquisitions, not atomic"},
    {"class": "perf", "severity": "minor", "lang": "rust",
     "pattern": "", "domain": "",
     "file": "highper-gateway/src/discovery/registry.rs", "line": 52,
     "summary": "No stampede/singleflight guard: concurrent callers at TTL expiry all independently hit the discovery backend"},
    {"class": "correctness", "severity": "minor", "lang": "rust",
     "pattern": "", "domain": "",
     "file": "highper-gateway/src/discovery/registry.rs", "line": 288,
     "summary": "TTL test backdates by ttl+1s only; exact elapsed==ttl boundary (>= vs >) is never asserted, a mutation-testing blind spot"}
  ]
}
```